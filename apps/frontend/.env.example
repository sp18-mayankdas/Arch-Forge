# Frontend connects directly to the backend (no Vite proxy).
# These default to the local dev backend if unset — only needed to override for
# other environments (staging/prod). Vite only exposes vars prefixed with VITE_.
VITE_API_URL=http://localhost:3001
VITE_WS_URL=ws://localhost:3001
